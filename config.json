{
   "modules": {
        "osvinit": {
            "type": "direct-dir",
            "path": "${OSV_BASE}/modules/cloud-init/"
        },
    "repositories": [
        "${OSV_BASE}/apps",
        "${OSV_BASE}/modules"
        , "${OSV_BASE}/../mike-apps"
        , "${OSV_BASE}/../openfoam-cases"
    ]
   },
   "default": [ "cloud-init", "httpserver" ]
}
